import Item from "@/components/Item"

export default function ItemList() {
    
    const itemList = ["hello"]

    return (
        <div>
            {/* {itemList.map((item) => (<Item item={item} />))} */}
        </div>
    )
}